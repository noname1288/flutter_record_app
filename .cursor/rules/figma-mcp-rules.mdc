---
description: Figma MCP server rules
globs:
alwaysApply: true
---

# Figma MCP Rules

- The Figma MCP server provides an assets endpoint which can serve image and SVG assets
- IMPORTANT: If the Figma MCP server returns a localhost source for an image or an SVG, use that image or SVG source directly
- IMPORTANT: DO NOT import/add new icon packages, all the assets should be in the Figma payload
- IMPORTANT: do NOT use or create placeholders if a localhost source is provided
- IMPORTANT: If a design uses image/icon assets from Figma and they are not in the repo yet, explicitly tell the user to download/export those assets from Figma and add them to the project assets.
