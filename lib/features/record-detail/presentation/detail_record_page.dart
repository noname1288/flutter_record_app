import 'package:flutter/material.dart';

class DetailRecordPage extends StatelessWidget {
  const DetailRecordPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Detail Record'),
      ),
      body: Center(
        child: Text('Detail Record'),
      ),
    );
  }
}